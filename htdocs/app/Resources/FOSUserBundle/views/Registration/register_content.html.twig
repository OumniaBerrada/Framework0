{% trans_default_domain 'FOSUserBundle' %}
<form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="form-inline">
    {{ form_widget(form) }}
    <div>
        <input type="submit" id="_submit" name="_submit" class="btn btn-warning" value="Valider" />
    </div>
</form>
